# Current Server Architecture

> **The name of this file is misleading and nothing links to it.** It is not a
> copy of `server.mjs` (that file is now ten lines). It is an architecture
> overview, in Markdown, with a `.txt` extension. Checked 2026-08-09: no other
> document references `docs/server_mjs.txt`, so it had drifted unread — which is
> how the privacy section below came to describe a safeguard that had been removed.
> Renaming it to `docs/ARCHITECTURE.md` is the obvious fix and is left as a
> deliberate change rather than done in passing.

## Overview

`comment-bank-api/server.mjs` is now a thin application entrypoint. It loads
configuration, runs Umzug migrations, creates the Express app from
`src/app.js`, and starts listening on `config.port` (default `44344`).

The active backend is no longer a monolithic `server.mjs` file. Runtime code is
split across:

- `src/app.js`: Express app setup, CORS, session middleware, static files.
- `src/routes/index.js`: API route registration and request handlers.
- `src/models/index.js`: Sequelize model definitions and associations.
- `src/db/sequelize.js`: MariaDB/MySQL Sequelize connection.
- `src/db/migrate.js`: Umzug migration runner.
- `src/services/openai.js`: OpenAI client setup.
- `src/services/dbBackup.js`: guarded `mysqldump` backup/export helpers.

## Runtime Stack

- Node.js + Express 5.
- MariaDB/MySQL through Sequelize.
- Umzug migrations in `comment-bank-api/migrations/`.
- `connect-session-sequelize` for persistent sessions in the `Sessions` table.
- OpenAI Responses API through `openai.responses.parse`.
- Static frontend pages served from `comment-bank-api/public/`.

## Configuration

Environment variables are loaded from `comment-bank-api/.env`. The full list is
documented in `README.md` and mirrored in `comment-bank-api/.env.example`.

Important defaults:

- `PORT=44344`
- `DB_DIALECT=mariadb`
- `OPENAI_MODEL=gpt-5.2`
- `OPENAI_REASONING_EFFORT=none`
- `ENABLE_DB_BACKUP=false`

## Data Model

The core models are:

- `User`: username, bcrypt password hash, `isAdmin`.
- `Subject`: global subject list.
- `YearGroup`: global year-group list.
- `Category`: comment-bank category scoped to `userId`, `subjectId`,
  `yearGroupId`.
- `Comment`: comment text linked to a category.
- `Prompt`: per-user prompt text scoped to subject/year group.
- `SubjectContext`: per-user subject description and optional word limit scoped
  to subject/year group.
- `UserSubject`: user-selected visible subjects.
- `UserYearGroup`: user-selected visible year groups.

`Category`, `Prompt`, and `SubjectContext` are user-scoped. This matters for any
admin feature that writes comments for another staff member: the target staff
member's `User.id` must be used as the owner, not the admin's session user id.

## Main API Areas

> **This is an index, not an inventory.** Measured 2026-08-09 by enumerating
> `app.router.stack` at runtime: the app registers **68** routes and the lists
> below name **44**. Nothing documented here is phantom — every listed route
> exists — but 24 are missing, most of them the admin target-staff endpoints added
> later (`/api/admin/staff/:userId/…`) plus `GET /api/version`, `GET /api/health/db`,
> `GET /api/user-settings`, `GET /api/user-selected-settings`, `POST /api/user-subjects`,
> `POST /api/user-year-groups`, `GET /api/categories/:id` and `GET /api/comments/:id`.
>
> A hand-kept list of 68 routes will drift again, so **the authoritative list is
> the runtime enumeration** in `comment-bank-api/tests/route-auth-matrix.test.js`
> — that test walks the real router and fails when a route appears without a
> guard. Use the groupings below to find your way around, not to answer "does
> endpoint X exist".

### Auth and Sessions

- `GET /api/health`
- `POST /api/register`
- `POST /api/login`
- `POST /api/admin/login`
- `POST /api/logout`
- `GET /api/authenticated`
- `GET /api/user-info`
- `POST /api/change-password`

### Admin

- `GET /api/users`
- `POST /api/users`
- `DELETE /api/users/:username`
- `PUT /api/admin/user/:username/password`
- `GET /api/export-database`
- `POST /api/backup-database`
- Legacy duplicate admin routes also exist under `/api/admin/*` for subjects,
  year groups, users, export, and backup.

### Subjects and Year Groups

- `GET /api/subjects`
- `POST /api/subjects` (admin only)
- `PUT /api/subjects/:id` (admin only)
- `DELETE /api/subjects/:id` (admin only)
- `GET /api/year-groups`
- `POST /api/year-groups` (admin only)
- `PUT /api/year-groups/:id` (admin only)
- `DELETE /api/year-groups/:id` (admin only)

### Comment Banks

- `GET /api/categories-comments?subjectId=&yearGroupId=`
- `POST /api/categories`
- `PUT /api/categories/:id`
- `DELETE /api/categories/:id`
- `GET /api/comments?categoryId=`
- `POST /api/comments`
- `PUT /api/comments/:id`
- `DELETE /api/comments/:id`
- `POST /api/move-comment`

These routes currently operate on the logged-in user's comment bank.

### Prompts and Subject Context

- `GET /api/prompts?subjectId=&yearGroupId=`
- `GET /api/prompts/:subjectId/:yearGroupId`
- `POST /api/prompts`
- `PUT /api/prompts/:subjectId/:yearGroupId`
- `PUT /api/prompts/:id`
- `DELETE /api/prompts/:id`
- `DELETE /api/prompts/:subjectId/:yearGroupId`
- `GET /api/subject-context?subjectId=&yearGroupId=`
- `POST /api/subject-context`

Prompts and subject context are user-scoped.

### Report Generation and Imports

- `POST /generate-report`
- `POST /api/import-reports`
- `GET /api/export-categories-comments?subjectId=&yearGroupId=`
- `POST /api/import-categories-comments`

`/generate-report` and `/api/import-reports` are rate limited because they call
OpenAI. Imports from pasted reports merge with existing categories/comments for
the logged-in user. CSV import replaces the logged-in user's categories/comments
for the selected subject/year group.

## OpenAI and Privacy Controls

OpenAI calls use the Responses API with strict JSON schema output parsing.

Privacy safeguards currently in code:

- **Report generation:** the *current pupil's* name is redacted **in the browser**
  and never transmitted. `public/report-selection.js` swaps it for `PUPIL_NAME`
  before sending and swaps it back on the response. The server has no
  `redactPupilName` helper any more and **rejects** a request that carries a name
  with a 400 (since 2026-07-30). It follows that the server cannot double-check
  this — correctness rests on the browser code, which is why
  `tests/ui-redaction.test.js` matters more than its size suggests.
- **Report import:** there is **no name substitution at all**. The
  `pupilNames` mechanism was removed on 2026-08-06 by owner decision — teachers
  are not to enter pupil names, and no pupil-name list is to be held server-side.
  Pasted report text reaches the model as typed. Both import pages carry guidance,
  a **warn-only** suspect-name highlighter, and a confirm-before-send preview.
- `store: false` is sent with OpenAI requests — all five
  `openai.responses.parse` call sites (two in `src/routes/index.js`, three in
  `src/services/reportImport.js`) spread `buildOpenAIParams`, and this is now
  enforced rather than merely true; see the note below.
- `safety_identifier` is a SHA-256 hash of the session user id, from the same
  helper.
- Subject descriptions are used as context and explicitly excluded from report
  output.

> **Read the framing in `/workspace/CLAUDE.md` before changing any of the above.**
> The mitigation is *"a mitigation with an accountability trail"*, **not** *"names
> never reach the model"*. Other pupils' names cannot be auto-redacted — no roster
> is held, by design — and the highlighter is a heuristic, so **no warning
> appearing does not mean no name is present**. The explicit basis for accepting
> that residual risk is that teachers are expected not to enter another pupil's
> name. Detail: `docs/REDACTION-DECISIONS.md`, `docs/PROJECT_STATE.md` §6.3.1–6.3.2.
>
> This section previously claimed *"Pupil names are replaced with `PUPIL_NAME`
> before report imports and report generation prompts."* That described a
> server-side safeguard that no longer exists on either path — a stale claim in the
> dangerous direction, since it would have let a reader assume a protection the
> code does not provide.

**Now enforced.** A sixth `openai.responses.parse` written without spreading
`buildOpenAIParams` would default `store` to true and send no `safety_identifier`.
`comment-bank-api/tests/openai-privacy-params.test.js` drives every
OpenAI-calling path with a recording stub and asserts what was actually sent, and
separately counts the call sites so a new one cannot slip past unexercised. See
`PROJECT_STATE.md` §6.14.

## Admin Uploads

The app already stores comment banks per staff user, subject, and year group,
and admin target-staff endpoints now allow an admin to import previous reports
for a selected staff user. These endpoints write categories/comments to the
target staff user's `userId`, not the admin user's `userId`, and successful
imports make the selected subject/year group visible to the target staff user.

See `docs/admin_staff_report_upload_plan.md` for implementation and deployment
safety details.
